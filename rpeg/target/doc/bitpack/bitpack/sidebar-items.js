window.SIDEBAR_ITEMS = {"fn":[["fitss","Returns true iff the signed value `n` fits into `width` signed bits."],["fitsu","Returns true iff the unsigned value `n` fits into `width` unsigned bits."],["gets","Retrieve a signed value from `word`, represented by `width` bits beginning at least-significant bit `lsb`."],["getu","Retrieve an unsigned value from `word`, represented by `width` bits beginning at least-significant bit `lsb`."],["maxs",""],["maxu",""],["news","Return a modified version of the unsigned `word`, which has been updated so that the `width` bits beginning at least-significant bit `lsb` now contain the signed `value`. Returns an `Option` which will be None iff the value does not fit in `width` signed bits."],["newu","Return a modified version of the unsigned `word`, which has been updated so that the `width` bits beginning at least-significant bit `lsb` now contain the unsigned `value`. Returns an `Option` which will be None iff the value does not fit in `width` unsigned bits."]]};